# Create the most basic pyproject.toml possible - no optional sections
basic_pyproject = '''[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mayini-framework"
version = "0.1.3"
description = "A comprehensive deep learning framework"
authors = [{name = "Abhishek Adari", email = "abhishekadari85@gmail.com"}]
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.21.0",
    "matplotlib>=3.5.0",
    "seaborn>=0.11.0",
    "tqdm>=4.64.0",
    "scikit-learn>=1.1.0"
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["mayini", "mayini.nn", "mayini.optim", "mayini.training"]
'''

print("✅ ULTRA-BASIC pyproject.toml created!")
print(f"📏 Length: {len(basic_pyproject)} characters")
print(f"📝 Lines: {len(basic_pyproject.split(chr(10)))}")

# Show each line with numbers
lines = basic_pyproject.split('\n')
for i, line in enumerate(lines, 1):
    print(f"Line {i:2d}: {line}")

print("\n🎯 REMOVED EVERYTHING COMPLEX:")
print("1. ✅ No optional-dependencies section")
print("2. ✅ No project.urls section") 
print("3. ✅ No setuptools.packages.find section")
print("4. ✅ Explicit package list instead of discovery")
print("5. ✅ Single-line author format")
print("6. ✅ No keywords or classifiers")
print("7. ✅ Absolute minimum required by PyPI")
